{
  "name": "spokiy-diary",
  "version": "1.0.0",
  "private": true,
  "description": "Спокій — щоденник тривожності. Сервер статики + REST API на вбудованому SQLite (node:sqlite).",
  "type": "commonjs",
  "main": "serve.js",
  "scripts": {
    "start": "node serve.js",
    "dev": "node serve.js",
    "validate:recovery": "node scripts/validate-recovery.js",
    "typecheck:recovery": "npx -p typescript@5.6.3 tsc -p tsconfig.recovery.json"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
